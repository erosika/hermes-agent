Charizard Agent payload.

This skin uses a static hero. If `charizard.png` is added to this folder, the
shared PNG-to-braille renderer will use it automatically with near-white pixel
masking; otherwise the built-in fallback hero art is used.
